<Window x:Class="QuizzApp.EditQuestionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuizzApp"
        mc:Ignorable="d"
  Title="Редактировать вопрос" Height="500" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Текст вопроса:" Margin="0,0,0,5"/>
        <TextBox x:Name="TbQuestion" Height="60" Grid.Row="1"/>

        <ListView x:Name="LvOptions" Grid.Row="2" Margin="0,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding OptionText}" Width="250"/>
                        <CheckBox Content="Правильный" IsChecked="{Binding IsCorrect}" Margin="10,0"/>
                        <Button Content="Удалить" Tag="{Binding}" Click="BtnDeleteOption_Click" Width="70"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Button Content="Добавить вариант" Click="BtnAddOption_Click" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Click="BtnSave_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
